name: Build and Release Plugin

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build plugin zip
        run: |
          mkdir -p dist/curly-events
          cp -r curly-events.php uninstall.php assets includes vendor dist/curly-events/
          rm -rf dist/curly-events/vendor/plugin-update-checker/.git
          cd dist && zip -r curly-events.zip curly-events

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/curly-events.zip
          generate_release_notes: true